version: '3'
services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 8000
    environment:
      - TELEGRAM_KEY=${TELEGRAM_KEY}
      - SPACY_HOST=${SPACY_HOST}
    depends_on:
      - spacy
  spacy:
    image: jgontrum/spacyapi:es_v2
    ports:
      - 80
